export type PaymentStatistics = {
    id: string,
    amount: number,
    amount_received: number,
    created: number,
    currency: string,
    uid: string,
    receipt_email: string,
    status: string,
}
